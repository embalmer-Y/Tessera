# SPDX-License-Identifier: Apache-2.0
CONFIG_ZTEST=y
CONFIG_TS_TEST=y
# SMP 真并行（DEC-43 并发回归判别力前提；posix 缺省即 builtin 原子，
# SMP 另需 USE_SWITCH——否则静默失效）
CONFIG_SMP=y
CONFIG_MP_MAX_NUM_CPUS=4
CONFIG_USE_SWITCH=y
CONFIG_TIMESLICING=y
