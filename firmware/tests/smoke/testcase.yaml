tests:
  framework.smoke:
    tags: framework
    integration_platforms:
      - native_sim
    # qemu_cortex_m3 = QEMU 板（注：4.4.0 该板 twister 元数据未迁移，当前仅构建级；
    # 运行级测试走 native_sim/CI，见 AGENTS.md 环境结论）；
    # nucleo_h743zi = 目标板（构建验证，build-only 用）。
    platform_allow:
      - native_sim
      - qemu_cortex_m3
      - nucleo_h743zi
